// Sentry wird automatisch initialisiert, sobald dieses Modul geladen wird
// (siehe src/lib/sentry.ts).
// Wir nennen es hier nur explizit, damit der TypeScript-Compiler die
// Abhängigkeit erkennt und es in der Bundle-Analyse erscheint.
import '../lib/sentry';
